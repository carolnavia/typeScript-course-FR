(() => {
  //const numbers: (number | string | boolean)[] = [1, 2, 3, 4, 5, "6", 7, 8];

  const numbers: number[] = [1, 2, 3, 4, 5, 6, 7, 8];

  const villians: string[] = ["Omega Rojo", "Dormammu", "Duende verde"];

  villians.forEach((v) => console.log(v.toUpperCase()));
})();
